{% set nextPageRegExp = r/\/\d+\/$/g %}

{% if pagination.nextPageHref and nextPageRegExp.test(pagination.nextPageHref) %}
    <div class="read-more-row">
        <button id="readMoreBtn">{% t 'buttons.load-more-articles' %}</button>
    </div>
{% endif %}
